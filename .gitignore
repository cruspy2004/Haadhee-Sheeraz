node_modules/
.next/
out/
build/
# Vite's output dir. Nothing produces it any more, but the deploy was
# previously configured to serve it — keep it from ever reappearing.
dist/
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
